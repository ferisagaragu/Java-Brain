<ngx-select 
                                            [(ngModel)]="modelRevisor"
                                            [disabled]="!canEdit || revisores.length == 1 || usr.roleId == 3"
                                            [items]="revisores" 
                                            [multiple]="false" 
                                            placeholder='Selecciona un revisor'
                                            [defaultValue]='modelRevisor'
                                            noResultsFound="No se encontraron resultados"
                                            (select)="selectRevisor($event)"
                                            name="revisor"
                                            #revisor
                                            required>
                                        </ngx-select>